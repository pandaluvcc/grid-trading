# 🚀 前端项目启动指南

## 前置条件

确保已安装 Node.js（建议 16.x 或更高版本）

检查是否安装：
```bash
node -v
npm -v
```

---

## 快速启动（3 步）

### 第 1 步：进入前端目录
```bash
cd E:\project\grid-trading\frontend
```

### 第 2 步：安装依赖（首次运行）
```bash
npm install
```

如果安装慢，使用国内镜像：
```bash
npm install --registry=https://registry.npmmirror.com
```

### 第 3 步：启动开发服务器
```bash
npm run dev
```

启动成功后，浏览器访问：**http://localhost:3000**

---

## 完整启动流程

```bash
# 打开 PowerShell 或 命令提示符
cd E:\project\grid-trading\frontend

# 首次安装依赖（只需运行一次）
npm install

# 启动前端开发服务器
npm run dev
```

**看到以下信息说明启动成功：**
```
  VITE v5.x.x  ready in xxx ms

  ➜  Local:   http://localhost:3000/
  ➜  Network: use --host to expose
  ➜  press h + enter to show help
```

---

## 测试前后端连接

1. **启动后端**（8080 端口）
   ```bash
   # 在 IDEA 中运行 GridTradingApplication
   ```

2. **启动前端**（3000 端口）
   ```bash
   cd frontend
   npm run dev
   ```

3. **访问前端页面**
   ```
   http://localhost:3000
   ```

4. **点击"测试后端连接"按钮**
   - 成功：显示绿色提示 "后端连接成功！"
   - 失败：检查后端是否启动

---

## 常用命令

| 命令 | 说明 |
|------|------|
| `npm install` | 安装依赖（首次运行） |
| `npm run dev` | 启动开发服务器（热更新） |
| `npm run build` | 构建生产版本 |
| `npm run preview` | 预览生产版本 |

---

## 常见问题

### ❓ 问题 1：npm 命令找不到

**原因**：未安装 Node.js

**解决**：
1. 下载 Node.js：https://nodejs.org/
2. 安装后重启终端
3. 验证：`node -v` 和 `npm -v`

---

### ❓ 问题 2：依赖安装失败

**解决方法 1**：使用国内镜像
```bash
npm install --registry=https://registry.npmmirror.com
```

**解决方法 2**：清理缓存重装
```bash
rd /s /q node_modules
del package-lock.json
npm install
```

---

### ❓ 问题 3：端口 3000 被占用

**解决**：修改 `vite.config.js` 中的端口
```javascript
server: {
  port: 3001  // 改为 3001 或其他端口
}
```

---

### ❓ 问题 4：页面空白

**检查**：
1. 浏览器控制台是否有错误（F12）
2. 后端是否启动（http://localhost:8080/health）
3. 前端是否启动（http://localhost:3000）

---

### ❓ 问题 5：后端连接失败

**检查清单**：
- [ ] 后端服务是否运行在 8080 端口？
- [ ] 访问 http://localhost:8080/health 是否返回 "OK"？
- [ ] 防火墙是否阻止了连接？

**测试后端**：
```bash
# 在浏览器或 PowerShell 中访问
curl http://localhost:8080/health
```

---

## 开发模式 vs 生产模式

### 开发模式（推荐）
```bash
npm run dev
```
- ✅ 热更新（代码修改自动刷新）
- ✅ 详细错误信息
- ✅ 快速启动

### 生产模式
```bash
npm run build    # 构建
npm run preview  # 预览
```
- ✅ 代码压缩优化
- ✅ 性能最佳
- ✅ 部署到服务器

---

## 项目结构说明

```
frontend/
├── node_modules/       # 依赖包（自动生成，不需要修改）
├── src/
│   ├── App.vue        # 主页面组件
│   └── main.js        # 应用入口
├── index.html         # HTML 模板
├── package.json       # 项目配置
├── vite.config.js     # Vite 配置（端口、代理等）
└── README.md          # 项目文档
```

---

## 下一步

前端启动成功后，可以：
1. 修改 `src/App.vue` 自定义页面
2. 添加新的 Vue 组件
3. 调用后端 API
4. 学习 Vue 3 和 Element Plus

**Vue 3 官方文档**：https://cn.vuejs.org/  
**Element Plus 文档**：https://element-plus.org/

---

**✅ 前端项目已就绪！开始开发吧！**
