# deploy/config.ps1 - 部署配置文件
# 请修改以下配置为你的京东云服务器信息

# ============================================
# 服务器连接信息
# ============================================

# 京东云服务器 IP 地址
$SERVER_IP = "117.72.157.115"

# SSH 用户名（通常是 root）
$SERVER_USER = "root"

# SSH 端口（默认 22）
$SERVER_PORT = 22

# ============================================
# 部署路径配置
# ============================================

# 服务器上的部署目录（你习惯的路径）
$REMOTE_PATH = "/data/docker/grid-trading"

# ============================================
# SSH 认证方式（二选一）
# ============================================

# 方式 1: SSH 密钥认证（推荐）
$SSH_KEY = "$env:USERPROFILE\.ssh\id_rsa"

# 方式 2: 密码认证（不推荐，仅测试用）
# 如果使用密码，请取消下面的注释并填入密码
# $SERVER_PASSWORD = "your-password"

# ============================================
# 构建配置
# ============================================

# Maven 可执行文件路径（如果不在 PATH 中）
$MAVEN_CMD = "mvn"

# Node.js npm 可执行文件路径（如果不在 PATH 中）
$NPM_CMD = "npm"


